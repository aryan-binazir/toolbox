# TOML config with an in-app raw editor

AI Scheduler will use `~/.config/ai-scheduler/config.toml` as the user-editable configuration file. The app will expose normal UI controls for common routine edits, plus an in-app raw config editor panel for runner and advanced configuration so the file remains the source of truth without forcing the user out to a separate editor for every change.
